apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: nginx-app
  namespace: argocd
spec:
  project: default
  source:
    repoURL: 'https://github.com/MidoriAppleCore/argoTestPub' # Replace with your Git repo URL
    targetRevision: HEAD
    path: ./nginx/
  destination:
    server: 'https://kubernetes.default.svc'
    namespace: default # Namespace where you want to deploy Nginx
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
